# tests calling methods of a class.
# the methods won't access any memers.
class TestClass 
	def foo()
		return 5 
	end 

	def TestClass()
	end
end

TestClass obj = TestClass()
return 1 if obj.foo() != 5 
