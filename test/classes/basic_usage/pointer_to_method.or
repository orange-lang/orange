# create a pointer to the method of a function
# and then call it passing in the this parameter.
class TestClass 
	int a

	def method()
		return a 
	end 

	def TestClass()
		a = 5 
	end 
end 

TestClass inst = TestClass()
var method = TestClass.method 
return 1 if method(&inst) != 5