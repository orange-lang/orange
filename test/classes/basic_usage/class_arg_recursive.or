class TestClass
	int a

	def foo(TestClass* ptr)
		return ptr.a
	end

	def TestClass(int a)
		this.a = a
	end
end

TestClass foo = TestClass(23)
TestClass bar = TestClass(56)

return 1 if foo.foo(&bar) != 56
