# 0% determined variables
def unknown_add(a, b)
	return a + b
end 

# 50% determined variables 
def template_add(a, int b) 
	return a + b
end 

# 100% determined variables 
def add(int a, int b)
	return a + b
end 

# repeat it 
return 1 if unknown_add(3, 5) != 8
return 1 if unknown_add(3, 5) != 8

return 1 if unknown_add(3, 5.0) != 8.0 
return 1 if unknown_add(3.0, 5) != 8.0 
return 1 if unknown_add(3.0, 5.0) != 8.0 
return 1 if unknown_add(3.0f, 5.0f) != 8.0 

return 1 if template_add(3, 5) != 8
return 1 if template_add(3, 5.0) != 8.0
return 1 if template_add(3.0, 5) != 8.0
return 1 if template_add(3.0, 5.0) != 8.0

return 1 if add(3, 5) != 8
return 1 if add(3.0, 5.0) != 8
return 1 if add(3.0f, 5.0f) != 8

return 0