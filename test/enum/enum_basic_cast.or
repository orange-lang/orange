enum Status
	PENDING
	RUNNING
	STOPPED
	EXITED
end

return 1 if (int)Status.PENDING != 0
return 2 if (int)Status.RUNNING != 1
return 3 if (int)Status.STOPPED != 2
return 4 if (int)Status.EXITED != 3
return 0
