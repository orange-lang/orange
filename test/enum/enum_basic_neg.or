enum Status
	PENDING = -2
	RUNNING = -1
	STOPPED
	EXITED
end

return 1 if Status.PENDING != -2
return 2 if Status.RUNNING != -1
return 3 if Status.STOPPED != 0
return 4 if Status.EXITED != 1
return 0
